# Conductor contract fixtures

These redacted fixtures mirror Conductor's published `/v0` OpenAPI response
objects and MCP documentation. They are contract fixtures, not evidence from an
authenticated acceptance run. Replace or extend them only after comparing the
exact release candidate with live MCP responses from both coordinator harnesses.

`models.json` follows the documented `list_models` semantics; that MCP-only
operation is not present in the public `/v0` OpenAPI document.
